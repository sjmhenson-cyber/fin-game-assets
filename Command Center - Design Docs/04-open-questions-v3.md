# Open Questions (v3)

> **v3 — supersedes 04-open-questions-v2.md. Delete v2 once confirmed.
> Errand/courier item resolved (shelved — no active peer-to-peer debt).**

1. **Closure moment for a slain boss** — currently just a greyed-out card.
   Given how central "slaying real debt" is, does it need a trophy room,
   permanent record, or other lasting payoff?
2. **Ledger-edit detection** — needs an actual diff/reconciliation design,
   not just a flavor message, so the Log doesn't develop silent gaps when
   the Sheet is hand-edited.
3. **Debt ownership tagging** — needs a concrete data-model plan (which
   field, how existing debts get assigned an owner retroactively).
4. **Principal-paid tracking** — needs a concrete plan for surfacing this
   as its own running total per player.
5. **Combat flavor assignment** — how does the auto-forge system decide
   whether a given boss gets the army-unit flavor or the ammo/weapons
   flavor? Random (like the hash-based name/creature system), or tied to
   something about the debt itself?
