# Open Questions (v2)

> **v2 — supersedes 04-open-questions.md (v1). Delete the v1 file once
> confirmed. Items 1–3 from v1 (Stamina/Vault, turn definition, "no
> stores") are now resolved and moved into 02-game-design-document-v2.md.**

1. **Errand/courier system** — small interpersonal debt (Venmo-type) has a
   verb (deliver) but no actual mechanic designed yet.
2. **Closure moment for a slain boss** — currently just a greyed-out card.
   Given how central "slaying real debt" is, does it need a trophy room,
   permanent record, or other lasting payoff?
3. **Ledger-edit detection** — needs an actual diff/reconciliation design,
   not just a flavor message, so the Log doesn't develop silent gaps when
   the Sheet is hand-edited.
4. **Debt ownership tagging** — new requirement from the milestone system;
   needs a concrete data-model plan (which field, how existing debts get
   assigned an owner retroactively).
5. **Principal-paid tracking** — new requirement from the $1,000 milestone;
   needs a concrete plan for surfacing this as its own running total per
   player.
